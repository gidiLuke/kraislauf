# Production environment settings - copy to .env.production and fill in secrets
# Common settings
COMPOSE_PROJECT_NAME=kraislauf

# Backend settings
ENVIRONMENT=production
LOG_LEVEL=info
PORT=8000

# Database settings
POSTGRES_USER=kraislauf
POSTGRES_PASSWORD=changeme_in_production  # Change this in the actual .env.production file
POSTGRES_DB=kraislauf
DB_HOST=db
DB_PORT=5432

# Frontend settings
NODE_ENV=production
NEXT_PUBLIC_API_URL=https://api.kraislauf.example.com  # Change to your actual domain